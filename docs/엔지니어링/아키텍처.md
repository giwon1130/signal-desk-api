# 백엔드 아키텍처

> **누가 봄**: 백엔드 개발자
> **언제 봄**: 첫 출근 / 새 service 추가 / 외부 client 변경
> **공유 부분**: app 저장소 [엔지니어링/아키텍처.md](https://github.com/giwon1130/signal-desk-app/tree/main/docs/엔지니어링/아키텍처.md) 도 같이 봐.

## 패키지 구조

```
com.giwon.signaldesk/
├─ bootstrap/                       # 셸 (Health / Cors / RateLimit / Security / App entry)
├─ features/
│  ├─ market/                       # 시장 데이터
│  │  ├─ application/               # service + 외부 client
│  │  ├─ presentation/              # controller + ApiResponse wrapper
│  │  └─ ...
│  ├─ workspace/                    # 사용자 데이터 (CRUD: 관심종목/포트폴리오/AI 픽)
│  ├─ auth/                         # 인증 (Email / Google / Kakao)
│  ├─ ai/                           # AI 픽 + 숨은 시그널
│  ├─ media/                        # Gemini 미디어 브리프 / 뉴스 종합 / 시황 인사이트
│  ├─ disclosure/                   # DART(KR) / SEC EDGAR(US) 공시 폴링
│  ├─ events/                       # 시장 이벤트 캘린더 (실적 등)
│  ├─ league/                       # 모의투자 리그 (immutable trade 원장 + derived position)
│  ├─ reading/                      # 리딩(애널리스트 콜) — 리더/구독/콜
│  ├─ realtime/                     # WS price ticker
│  ├─ push/                         # Expo push + 알림 설정
│  ├─ fortune/                      # 오늘의 운
│  └─ system/                       # 외부 의존성(Gemini) 헬스 노출
└─ ...
```

## 핵심 service

(stub)
| 클래스 | 책임 |
|--------|------|
| `MarketOverviewService` | 진입점. 캐시 + 동시 외부 호출 + 응답 조립 |
| `MarketSessionService` | KR/US 세션 + 휴장 캘린더 |
| `IndexChartFactory` | D/W/M 캔들 시뮬 |
| `NewsSentimentBuilder` | 뉴스 RSS → 키워드 sentiment |
| `AlternativeSignalService` | Pizza Index + Policy Buzz + Bar Counter-Signal |
| `WatchAlertService` | 관심종목 시그널 산출 |
| `WorkspaceEnrichmentService` | 사용자 데이터 + 라이브 시세 결합 |
| `RecommendationMetricsCalculator` | AI 추천 적중률/평균/최고/최저 |
| `DailyBriefBuilder` | 장전 개인화 브리핑 |
| `PersonalContextAnnotator` | 사용자 컨텍스트 주석 |
| `PickNewsMatcher` | AI 픽 ↔ 뉴스 매칭 |
| `MorningBriefService` / `IntradayBriefService` / `EveningBriefService` | Gemini 모닝(08:30)·마감(15:40)·이브닝(06:30) 브리프 생성 + 스케줄 (장중 12:30 정기 브리프는 제거, `MIDDAY` 슬롯은 수동 refresh 전용) |
| `NewsDigestService` | KR/US 뉴스 종합 요약 |
| `MarketInsightService` | 오늘의 시황 인사이트 (Gemini, 캐시) |
| `DartDisclosureService` / `SecEdgarDisclosureService` | KR/US 공시 5분 폴링 + dedup |
| `MarketEventService` | 시장 이벤트 캘린더 (Finnhub 실적 + 정적 이벤트) |
| `LeagueService` / `TradeService` / `PositionService` / `LeaderboardService` | 모의투자 리그 — trade 검증/체결, derived position, 순위 |
| `ReadingService` / `ReadingFeedService` / `ReadingPriceService` | 리딩 리더/구독/콜 박제, 피드, 콜 가격 추적 |
| `StockMentionDetector` | 리딩 본문 종목 자동 인식 |
| `AlertPreferenceService` | 사용자별 알림 설정 |

## 외부 client

(stub)
| 클라이언트 | 출처 | 호출 빈도 |
|-----------|-----|-----------|
| `KrxOfficialClient` | KRX 정보데이터 | core cache (60s/3m) |
| `NaverFinanceQuoteClient` | Naver | 5s WS + core cache |
| `NaverGlobalQuoteClient` | Naver Global | 5s WS |
| `NaverStockSearchClient` | Naver | 종목 검색 (10분 cache) |
| `CboeVixClient` | CBOE | core cache |
| `UsIndexService` | Yahoo→FRED | 미국 지수 단일 제공자 (야후 1순위, FRED 폴백, macro-index cache 30m) |
| `YahooQuoteClient` | Yahoo Finance v8 | 미국 지수(어젯밤 종가) + 글로벌 지수·선물(닛케이·항셍·S&P선물) |
| `FredIndexClient` | FRED | 미국 매크로 + 지수 폴백, core cache |
| `GoogleNewsRssClient` | Google News RSS | news cache (5m/15m) |
| `PizzIntClient` | PizzINT | 실험 지표 |
| `VenueSignalCollector` | 자체 | 실험 지표 |
| `GeminiClient` | Google Generative Language | 미디어 브리프/인사이트 생성 |
| `DartDisclosureClient` | OpenDART | KR 공시 5분 폴링 |
| `SecEdgarClient` | SEC EDGAR | US 공시 5분 폴링 |
| `FinnhubClient` | finnhub.io | US 실적 캘린더 |
| `NaverGlobalQuoteClient` | Naver Global | US 종목 현재가 |

## 캐싱 전략

(stub)
- `MarketOverviewService` 안에서 `cachedCore` (60s OPEN / 3m 휴장) + `cachedNews` (5m / 15m)
- `NaverStockSearchClient` 자체 ConcurrentHashMap (10분)
- 모두 in-memory. 단일 인스턴스 전제.

## 동시성

(stub)
- core cache 빌드 시 `synchronized` 단일 진입
- 외부 client 호출은 `CompletableFuture.supplyAsync` 로 병렬
- WS sessions / subscriptions: ConcurrentHashMap

## 저장소 (persistence)

(stub)
- Mode: `signal-desk.store.mode` (file / jdbc)
- file: `./data/signal-desk-store.json` (개발용)
- jdbc: PostgreSQL (Spring JDBC + Flyway like 마이그레이션 부팅 시 실행)
- repository 인터페이스: `SignalDeskWorkspaceRepository`

## 핵심 결정

(stub)
- 왜 캐시 60s: KRX/Naver 보호 + 5s WS 가 별도 채워줌
- 왜 휴장 모드 cache 늘림: 외부 호출 불필요 시간대
- 왜 file mode 도 지원: 로컬 개발 / DB 없는 환경
- 왜 OAuth audience 화이트리스트: 다른 클라이언트 ID 토큰 차단
- 왜 single instance 가정: 사용자 작은 단계 — scale-out 시 외부 cache (Redis) 필요
